$('#error_messages').html(<%= render("error_messages").to_json.html_safe %>);
$('#user_menus').html(<%= render("user_menus").to_json.html_safe %>);

<% if current_user.try :admin? %>
  $('.admin').show();
<% end %>
$('.dropdown-toggle').dropdown();
