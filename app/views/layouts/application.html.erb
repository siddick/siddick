<!DOCTYPE html>
<html>
  <head>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <link href="https://plus.google.com/103719245554204460362" rel="publisher" />
    <title><%= title %> - Siddick</title>
    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags unless @page_caching %>
  </head>
  <body class="<%= body_class %>">
    <header><%= render "topbar" %></header>
    <div class="container">
      <div class="content">
        <header class="page-header <%= post_page_header_class %>">
        <% if content_for? :page_header %>
          <%= yield :page_header %>
        <% else %>
          <h1><%= title %></h1>
        <% end %>
        </header>
        <div class="row">
          <div class="span9 main">
            <div id="error_messages"><%= render "error_messages" unless @page_caching %></div>
            <%= yield %>
          </div>
          <aside class="span3 sidebar">
            <%= render "sidebar" %>
          </aside>
        </div>
      </div>
    </div>
    <footer><p>My blog.</p></footer>
    <%= javascript_include_tag "application" %>
    <%= render "google_analytics" %>
    <%= javascript_tag "$.getScript('#{update_caching_path("js")}');" if @page_caching %>
  </body>
</html>
