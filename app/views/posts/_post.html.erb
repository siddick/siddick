<%= content_tag( :article, "data-id" => post.to_param, "data-updated-at" => post.updated_at ) do %>
  <% unless @post %>
    <h3><%= link_to post.title, post %></h3>
  <% end %>
  <%= post.content ? post.content.html_safe : post.raw_content %>
<% end %>
